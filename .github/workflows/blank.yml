name: Env Demo
on: push
env:
  APP_ENV: "dev"
jobs:
  env_demo :
    env:
      APP_ENV: "test"
    runs-on: ubuntu-latests
    steps:
      - name: Connecting to ENV
        run: echo "APP is connected to $APP_ENV"
